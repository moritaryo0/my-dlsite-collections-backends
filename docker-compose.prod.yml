# backend/docker-compose.prod.yml（APIのみ）
version: '3.8'
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile.prod
    env_file:
      - .env.prod
    ports:
      - "8000:8000"
    restart: always
